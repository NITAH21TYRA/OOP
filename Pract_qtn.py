#Create a function that returns the student's name,their cohort and the email adress
# Acess this function using any instance of the class.
def studentNameEmaiCohort(self):
    return f"{studentNameEmaiCohort}"
